name: Deploy Docker Container

on:
  push:
    branches:
      - main

jobs:
  pre-build: 
    runs-on: ubuntu-latest

    steps: 
      - name: Echo tree
        run: tree
  
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
  
      - name: Build Docker playlist-api-image
        run: docker build -t callspadeoz123/playlist-api-image:latest playlist-api/
      
      - name: Build Docker video-api-image
        run: docker build -t callspadeoz123/video-api-image:latest videos-api/
  
      - name: Log in to Docker Hub
        run: echo ${{ secrets.DOCKER_PASSWORD }} | docker login -u ${{ secrets.DOCKER_USERNAME }} --password-stdin
  
      - name: Push Docker playlist-api-image:latest to Docker Hub
        run: docker push callspadeoz123/playlist-api-image:latest

      - name: Push Docker videos-api-image:latest to Docker Hub
        run: docker push callspadeoz123/videos-api-image:latest
      